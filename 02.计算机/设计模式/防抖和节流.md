# 防抖和节流

防抖和节流时优化高频执行代码的一种手段。当 resize 、scroll、keypress、mousemove 等事件在触发时，会不断地调用保定在事件上的回调函数，极大地浪费资源，降低前端性能。

为了优化体验，需要对这类事件进行调用次数的限制，对此我没就可以采用防抖和节流。

## 防抖

n 秒后再执行该事件，若再 n 秒内重复触发，重新计时。

## 节流

n 秒内只运行一次，若在 n 秒内重复触发，只有一次生效。 