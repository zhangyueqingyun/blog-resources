# 序列的 JS 实现
## 序列类的实现
~~~javascript
class Sequence {
    constructor(arr) {
        this.arr = arr
    }

    /**
     * @ind 插入元素的位置
     * @val 插入元素的值
     */
    insert(ind, val) {
        for(let i = arr.length, i > ind, i--) {
            this.arr[i] = this.arr[i-1]
        }
        this.arr[i] = val
    }

    /**
     * @ind 待更新元素的下标
     * @val 新值
     */
    update(ind, val) {
        this.arr[ind] = val
    }

    /**
     * @val 待查找的值
     */
    find(val) {
        /** 见查找篇 */
    }

    sort() {
        /** 见排序篇 */   
    }

    distinct() {
        /** 见去重篇 */
    }
}

const sequence = new Sequence([2,5,7,9])
~~~